# Cogito RL stack.
# TRL only supports vLLM 0.13.0–0.23.0 — keep vllm inside that window.
# Versions are ranges (not hard pins) because cloud GPU images ship different
# CUDA/torch builds; setup.sh handles torch + flash-attn ordering separately.
vllm>=0.20.0,<0.23.1
trl>=0.18.0
transformers>=4.51.0
accelerate>=1.4.0
datasets>=3.2.0
deepspeed>=0.16.0
math-verify>=0.7.0
latex2sympy2-extended>=1.0.0
tensorboard>=2.16.0
matplotlib>=3.8.0
huggingface_hub>=0.34.0
